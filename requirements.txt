streamlit>=1. thirty8
pandas>=2.2.2
numpy>=1.26
pyarrow>=15.0.0        # for Parquet (read_parquet)
# fastparquet>=2024.2.0  # optional Parquet engine (keep pyarrow as primary)

folium>=0.16.0
streamlit-folium>=0.20.0

plotly>=5.22.0         # interactive charts in Streamlit
matplotlib>=3.8.0      # small PNG charts for popups

# kaleido>=0.2.1        # OPTIONAL: only if you ever export Plotly figs to PNG
